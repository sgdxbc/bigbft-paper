\section{Related Works}
\label{sec:rel}

\paragraph{Reduce storage overhead in state machine replication.}
As previously mentioned in \autoref{sec:bg}, horizontal scaling is a common practice in distributed storage and databases to handle large data volumes~\cite{gfs,bigtable,spanner}.
In these systems, data is partitioned into multiple shards which are individually replicated, similar to the sharded blockchains.

Several prior works have explored reducing storage overhead in state machine replication with crash-fault tolerance.
RS-Paxos~\cite{mu2014paxos} and CRaft~\cite{wang2020craft} utilize erasure coding to replicate the logs.
Only the leader node stores full logs, while the followers maintain coded fragments.
Racos~\cite{zarnstorff2024racos} further introduces leaderless replication to resolve the bottleneck on the leader, which achieve storage scalability.
The demand for Byzantine fault tolerance substantially complicates the challenge of reducing storage overhead.
In these systems, usually only the proposing node executes the transactions, while other nodes store the coded fragments of the logs to ensure the state is recoverable after the proposer fails.
Under Byzantine faults, the single execution result cannot be trusted, which brings the dilemma between security and storage scalability in \autoref{sec:bg}.

\paragraph{Storage scalable solutions for cryptocurrencies.}
Vault~\cite{leung2018vault} proposes a sharded storage solution for account-based blockchains.
With the stateless approach, Utreexo~\cite{dryja2019utreexo} proposes a stateless blockchain for UTXO based cryptocurrencies, while \textsc{Edrax}~\cite{chepurnoy2018edrax} is capable for both UTXO and account-based models.
In this scenario, the clients hold the piece of state relevant to themselves (e.g., the UTXOs they own or their account balances) and the storage nodes in general stateless blockchains are unnecessary.
All these works are specific to cryptocurrencies.

\paragraph{Execute-order blockchains.}
Hyperledger Fabric~\cite{hyperledger} introduces the execute-order-validate paradigm for permissioned blockchains.
The execution is performed on few designated execution peers before ordering, and the updates are then ordered by the consensus layer and applied to the storage nodes.
While the consensus is secure, the execution must be performed by sufficient peers to ensure correctness, and the state must be stored by sufficient storage nodes to ensure availability.
Thus, Fabric also suffers from trading off storage scalability and security.

\paragraph{Optimize state storage on nodes.}
Several methodologies aim to optimize state storage on nodes without altering the fundamental architecture of blockchains.
COLE~\cite{zhang2024cole} designs a column-based learned index for Blockchain systems and reports 94\% reduction in state storage overhead.
Vault~\cite{leung2018vault} also proposed an expiration mechanism to prune zero-balance accounts.
These techniques can be orthogonally applied to \sys to further reduce the storage overhead on nodes.

\paragraph{Scaling BFT protocols.}
Numerous research efforts have been dedicated to scaling BFT consensus protocols to accommodate a larger number of nodes.
SBFT~\cite{gueta2019sbft}, Narwhal~\cite{narwhal} and Autobahn~\cite{giridharan2024autobahn} improve scalability of transaction dissemination.
Some sharded BFT protocols like \textsc{Elastico}~\cite{luu2016secure}, \textsc{Ohie}~\cite{yu2020ohie} and Monoxide~\cite{wang2019monoxide} partition the consensus layer, so that each transaction is ordered by a subset of nodes.
Monoxide further partitions the execution with a cross shard relaying mechanism.
These works can improve the ordering (and execution for some works) scalability, but they do not reduce the storage overhead on each node.

% \paragraph{Stateless blockchains.}
% Several methodologies advocate for nodes to maintain \emph{no} state.
% The storage overhead per node remains constant, irrespective of state size.
% Piperine~\cite{lee2020replicated} aims to delegate state storage and execution to an external, untrusted prover.
% The state is structured using ADS, and the prover generates succinct execution proofs verifiable by the nodes.
% Consequently, nodes effectively "execute" transactions through verification.
% \textsf{SlimChain}~\cite{xu2021slimchain} employs verifiable computation~\cite{ben2014succinct,costan2016intel} to accommodate diverse transaction workloads and facilitate efficient storage sharding.
% Within permissionless scenarios, rollups~\cite{ethereum_scaling_docs} represent the primary scaling solution for Ethereum.
% These rollups store state and execution transactions off-chain, periodically submitting state commitments and executed transactions to the blockchain.
% Security is ensured either through verifiable computation or decentralized auditing, in conjunction with the inherent security of the blockchain itself, i.e., the consensus on submitted commitments and transactions.

% Although stateless blockchains can attain optimal storage scalability on the nodes, they necessitate the introduction of supplementary components to the system.
% The nodes are no longer self-sufficient in ensuring liveness, but rather depend on external executors.
% Certain methodologies also delegate transaction ordering externally, in addition to execution.
% In such scenarios, the nodes must additionally safeguard against potential discrimination, such as censorship, by external \emph{sequencers}.
% Notwithstanding these considerations, \sys can also be employed to decentralize rollups, which currently exhibit relative centralization.

% \paragraph{Availability of historical transactions, i.e., blocks.}
% In contrast to prior research maintaining partial states on nodes, as discussed in \autoref{sec:bg}, certain other studies propose availability solutions for historical transactions.
% All committed transactions are preserved for an extended duration, and the state (either the current iteration or any prior one) can consistently be reconstituted by re-executing the transactions.

% In public blockchains, \emph{archival nodes} voluntarily supply historical blocks for auditing or analytical purposes.
% BFT-store~\cite{qi2020bft} constitutes a space-efficient block-availability solution tailored for permissioned blockchains.
% Blocks undergo erasure coding and subsequent distribution across nodes to achieve storage scalability; this work demonstrates a fault tolerance of $f$ faulty nodes out of a total of $3f+1$ while ensuring block availability.
% Danksharding~\cite{eip4844} proposes augmenting Ethereum's blocks with \emph{blobs}, which are erasure coded and collaboratively stored by the nodes.
% Distinct from BFT-store, Ethereum ensures blob availability only for a defined period (e.g., currently 18 days), as the rationale for blob storage pertains to auditing rather than archival.

% On one hand, scaling block storage solutions is simpler than scaling state storage solutions.
% The historical blocks are immutable and certainly structured in an append-only list, while the state is a piece of arbitrary data that is both added with new content and modified in place.
% Thus, collaborative block storage is a simpler approach than state management system on reasoning about the availability.
% On the other hand, the block data is much more than the state and also increases much faster.
% It's infeasible to store the whole block history forever by the operating nodes of the blockchains.
% Also, many blockchain systems require to \emph{validate} transactions before proposing them in the network.
% The validators still need to store the whole states, and a scalable state management solution is still required if the state size is beyond the validators' storage capacities.
